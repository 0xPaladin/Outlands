//added to perilous.js @ 12424 and 11028
const PSDataManager = (map, view) => {
  window.PSView = view;
  window.PSMap = map;

  //console.log(window.PSView)
  //PSView.view.compass.set_visible(1);
  //view.view.toggleCompass();
}
